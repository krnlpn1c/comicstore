spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: validate

application:
  client:
    rating-rest:
      base-url: http://localhost:${wiremock.server.port}
